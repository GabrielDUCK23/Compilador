
// Declaración de una variable con tipo explícito
let miNumero: number = 42;
let miCadena: string = "Hola, TypeScript";
let miBooleano: boolean = true;

// Función que toma dos números y devuelve su suma
function sumar(a: number, b: number): number {
  return a + b;
}

// Llamada a la función y uso de los resultados
let resultado: number = sumar(10, 20);
console.log(`La suma es: ${resultado}`);
